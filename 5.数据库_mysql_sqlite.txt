mysql  --  sqlite
库：                          
增：create database x;          sqlite3  x.db;
删：drop database x;
查：show databases;
使用数据库：use x;

表：
增：create table tb(id bigint unsigned primary key auto_increment,name varchar(20));
   create table tb(id integer primary key autoincrement,name varchar(20));
删：drop tables tb;
查：show tables;                .tables

数据：
增：insert into tb(name) values("tom");
删：delete from tb where name = "tom";
改：update tb set name = "mary" where name = "tom";
查：select * from tb;

索引：
1.普通索引 create index index_name on tb(name);
          drop index index_name on tb;                       drop index index_name;
          show index from tb;                                .indexes tb
2.唯一索引 create unique index index_name on tb(name);
3.主键索引 primary key
4.组合索引 create index index_name on tb(id,name);
5.全文索引 create fulltext index index_name on tb(name);       无
       
